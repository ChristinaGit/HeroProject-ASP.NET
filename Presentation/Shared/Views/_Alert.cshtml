@using HeroProject.Presentation

@{
    var message = TempData.GetMessage();
    if (message != null)
    {
        <div class="alert alert-success alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close" fade in>&times;</a>
            @message
        </div>
    }
}